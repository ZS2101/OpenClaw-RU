import{createStandardChannelSetupStatus,DEFAULT_ACCOUNT_ID,setSetupChannelEnabled}from"openclaw/plugin-sdk/setup";import type{ChannelSetupWizard}from"openclaw/plugin-sdk/setup";import{resolveVKTeamsAccount}from"./accounts.js";import{resolveVTToken}from"./token.js";import{VT_HELP,vtSetupAdapter,isVTConfigured}from"./setup-core.js";const CH="vkteams"as const;export const vtSetupWizard:ChannelSetupWizard={channel:CH,status:createStandardChannelSetupStatus({channelLabel:"VK Teams",configuredLabel:"configured",unconfiguredLabel:"needs token",configuredHint:"configured",unconfiguredHint:"corporate messenger · needs token",configuredScore:1,unconfiguredScore:5,resolveConfigured:({cfg})=>isVTConfigured(cfg,DEFAULT_ACCOUNT_ID)}),credentials:[{inputKey:"token",providerHint:CH,credentialLabel:"токен бота VK Teams",preferredEnvVar:"VKTEAMS_BOT_TOKEN",helpTitle:"VK Teams bot token",helpLines:VT_HELP,envPrompt:"VKTEAMS_BOT_TOKEN detected. Use env var?",inputPrompt:"Введите токен бота VK Teams",allowEnv:({accountId})=>accountId===DEFAULT_ACCOUNT_ID,inspect:({cfg,accountId})=>{const a=resolveVKTeamsAccount({cfg,accountId});return{accountConfigured:Boolean(resolveVTToken(a)),hasConfiguredValue:Boolean(a.token?.trim()),resolvedValue:a.token?.trim()??"",envValue:accountId===DEFAULT_ACCOUNT_ID?process.env.VKTEAMS_BOT_TOKEN?.trim():undefined}}}],finalize:async()=>{},disable:(cfg)=>setSetupChannelEnabled(cfg,CH,false)};export{vtSetupAdapter};
